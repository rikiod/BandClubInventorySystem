# This program generates random passwords with a length of 20. The number provided by the user specifies the number of passwords to be generated.

import random
import string

n = int(input("Please input how many passwords you would like to be generated: "))

for i in range(n):
    pwString = string.ascii_letters + string.digits + string.punctuation
    for i in range (20):
      print('Password', i + 1, ':', ''.join(random.choice(pwString) 
